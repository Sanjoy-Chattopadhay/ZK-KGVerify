# ---- machine learning ----
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.65.0

# ---- zero-knowledge proof layer (BN128) ----
py_ecc>=6.0.0

# ---- Ethereum: Sepolia deployment and on-chain verification ----
web3>=6.0.0
eth-account>=0.10.0
eth-utils>=2.0.0
py-solc-x>=2.0.0

# ---- in-memory EVM used by tools/test_onchain_verifier.py ----
# Required: run_all.py verifies the contract locally before it spends any
# testnet ETH, so this is part of the default path rather than an extra.
#
# eth-tester has never published a stable release -- every version on PyPI is
# a beta. Newer pip falls back to pre-releases when nothing stable matches,
# but older pip (Colab's, for one) refuses and the install fails. Naming a
# beta explicitly opts this requirement into pre-release resolution on every
# pip, with no --pre flag needed.
eth-tester[py-evm]>=0.13.0b1
